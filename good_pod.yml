apiVersion: v1
kind: Pod

metadata:
name: nginx-good

spec:
containers:

* name: nginx

  image: nginx

  resources:

  requests:
  cpu: "100m"
  memory: "128Mi"

  limits:
  cpu: "500m"
  memory: "512Mi"
